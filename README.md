# Vega CRM

Система контроля объектов по зачистке резервуаров для компании "Вега".

## 🎯 Функционал

### Для администратора (Степан):
- Диаграмма Ганта всех объектов
- Контроль сроков выполнения
- Просмотр ежедневных отчетов мастеров
- Управление пользователями

### Для мастеров:
- Мобильное PWA приложение
- Ежедневные отчеты с фото
- Оффлайн-работа + синхронизация
- Учет инструментов

### Для заказчиков:
- Ограниченный доступ к прогрессу
- Автоматические отчеты
- Прозрачность работ

## 🛠️ Технологии

### Backend:
- **FastAPI** - современный Python фреймворк
- **PostgreSQL** - надежная база данных
- **SQLAlchemy** - ORM для работы с БД

### Frontend:
- **React + TypeScript** - веб-админка
- **PWA** - мобильное приложение для мастеров
- **Chart.js** - диаграмма Ганта

### Хостинг:
- **Railway** - облачная платформа
- **PostgreSQL на Railway** - база данных

## 🚀 Быстрый старт

### 1. Установка зависимостей:
```bash
pip install -r requirements.txt
```

### 2. Настройка базы данных:
Создать файл `.env`:
```env
DATABASE_URL=postgresql://user:password@localhost/vega_crm
SECRET_KEY=your-secret-key-here
```

### 3. Запуск сервера:
```bash
python main.py
```

Сервер запустится на `http://localhost:8000`

## 📁 Структура проекта

```
vega-crm/
├── main.py              # Основной файл FastAPI
├── requirements.txt     # Зависимости Python
├── README.md           # Документация
├── .env               # Переменные окружения
├── frontend/          # React фронтенд
│   ├── src/
│   ├── public/
│   └── package.json
└── pwa/              # PWA для мастеров
    ├── manifest.json
    └── service-worker.js
```

## 🔧 API Endpoints

### Основные:
- `GET /` - Проверка работы API
- `GET /api/health` - Проверка здоровья системы
- `GET /api/objects` - Получить все объекты
- `POST /api/objects` - Создать новый объект
- `GET /api/gantt` - Получить данные для диаграммы Ганта

### Аутентификация:
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/register` - Регистрация
- `GET /api/auth/me` - Информация о текущем пользователе

### Отчеты:
- `GET /api/reports` - Получить все отчеты
- `POST /api/reports` - Создать отчет
- `GET /api/reports/{object_id}` - Отчеты по объекту

## 📊 База данных

### Основные таблицы:
1. **users** - Пользователи системы
2. **objects** - Объекты (резервуары)
3. **daily_reports** - Ежедневные отчеты мастеров
4. **gantt_items** - Задачи для диаграммы Ганта

## 🎨 Диаграмма Ганта

Система автоматически создает диаграмму Ганта на основе:
- Дат начала/окончания объектов
- Прогресса выполнения
- Статусов объектов

## 📱 PWA для мастеров

### Особенности:
- Работает оффлайн
- Устанавливается на телефон
- Простой интерфейс для ежедневных отчетов
- Фотоотчеты с камеры телефона

## 🔒 Безопасность

- JWT токены для аутентификации
- Хеширование паролей
- Ролевая модель доступа
- HTTPS в продакшн

## 🚀 Развертывание

### На Railway:
```bash
railway up
```

### На собственном сервере:
```bash
docker-compose up -d
```

## 📞 Контакты

Система разработана для компании "Вега".
По вопросам разработки и поддержки обращаться к создателю системы.