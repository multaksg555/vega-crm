{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install fastapi uvicorn[standard]"
  },
  "deploy": {
    "startCommand": "python hello_railway.py",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30
  }
}
