{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install flask"
  },
  "deploy": {
    "startCommand": "python ultra_simple_railway.py",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30
  }
}
