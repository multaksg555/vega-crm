{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install fastapi uvicorn"
  },
  "deploy": {
    "startCommand": "python test_simple_server.py",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30
  }
}
